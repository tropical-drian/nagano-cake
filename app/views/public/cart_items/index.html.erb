<h2>ショッピングカート</h2>


<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
      <th></th>
    </tr>
　</thead>
　<tbody>
  <% @cart_items.each do |cart_item| %>
  <tr>
    <td>
    <%= attachment_image_tag( cart_item.product, :image, size: "20x20", fallback: "no_image.jpg") %>
    <%= cart_item.product.name %>
    </td>
    <td>
      <%#= cart_item.product_intax %>(<%#= cart_item.product.price %>)
    </td>
    <td>
    　<%= form_with model: @cart_item, url: cart_items_path , method: :patch ,local: true  do |f| %>
    　<%= f.number_field :quantity, size: 10, min: 1 %>
    　<!--< f.submit "変更"　%>-->
    　<% end %>
    </td>
    <td>
  　　小計
　　</td>
　　<!--<td><= link_to " 削除する", customers_cart_item_path(cart_item), method: :delete　%></td>-->
  </tr>
  <% end %>
  </tbody>
</table>
<div class ="action">
    <%= link_to " 買い物を続ける", products_path %>
</div>
<div>
  <p>合計金額</p>
  <!--<p><= total_price(@cart_items) %></p>-->
</div>
<div>
  <!--<= link_to " 情報入力に進む", new_customers_order_path %>-->
</div>